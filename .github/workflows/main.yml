name: Release
on:
  push:
    #tags:
    #  - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10, etc.
    branches:
      - main
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}
#  publish:
#   name: Publish to PyPI
#    runs-on: ubuntu-latest
#    steps:
#    - name: Checkout code
#      uses: actions/checkout@v2
#    - name: Build Docker image
#      run: docker build -t my-image-name .
#    - name: Bump version
#      uses: marvinpinto/action-automatic-semantic-version@v1
#      with:
#        repo_token: ${{ secrets.GITHUB_TOKEN }}
#    - name: Publish to PyPI
#      run: docker run --rm -v $PWD:/app my-image-name make publish-prod
#      env:
#        PYPI_USERNAME: __token__
#        PYPI_PASSWORD_PROD: ${{ secrets.PYPI_API_TOKEN }}
